1  2つのコマンドライン引数 x, y を受け取り、文字列 x を y 行繰り返し標準出力上に表示するプログラム repeat を作成せよ

2  2つの char* 型のポインタ s, d を引数にとり、s で指定される文字列から空白文字を全て取り除いた文字列を d で指定されるメモリ領域に出力する関数 trim(s, d) を定義せよ

3  以下のデータ型 Person で示される値をヒープメモリ上に作成し、その先頭アドレスを返す関数 create_person(name, age) を定義せよ

4  32ビットの符号なし整数 x を引数に取り、 x のビット13を0に、 ビット20を1に変えた値を返す関数 changebits(x) を定義せよ

5  2つの文字列へのポインタ s, suffix を引数にとり、文字列 s の末尾が文字列 suffix となっているかを真偽値として返す関数 endswith(s, suffix) を定義せよ
  